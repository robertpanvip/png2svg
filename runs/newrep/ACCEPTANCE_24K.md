# 新表示 24k 验收报告（ACCEPTANCE_24K）

时间：2026-09-10 01:35 ｜ ckpt：`runs/newrep/last.pt`（24k 从零）｜ 诊断：`diag_new.py`，30 场景 seed=777000，GPU

## 结论：**PASS**（三项判据全过），已启动 40k 续训

## 验收表

| 指标 | 判据 | 实测 | 旧表示基线（24k） | 判定 |
|---|---|---|---|---|
| mae_mean | ≤ 0.085 | **0.0541** | 0.069（同口径） | ✅（且优于旧基线 22%） |
| 对象匹配率 | ≥ 0.5 | **1.000（132/132）** | 总匹配率 0.32 | ✅ 满分 |
| 预测段类型多样性 | ≥3 种非 M | **C:485 / L:109 / A:60**（+M:132） | 240/240 全 blob | ✅ 无坍缩 |
| CPU total p95 | < 1s | **39.8ms**（under_1s=1.0，net 26.5ms） | ~20-27ms（旧表示） | ✅ |
| oracle_seg | 参考项 | +0.0016 | — | 类型分支接近 GT |
| nseg_gap | 参考项 | −1.78（段数偏少） | — | 待续训改善 |

## 短板（40k 续训目标）

1. **Q 段预测为 0**：模型用 C（三次）替代了所有 Q（二次）场景——渲染上 C 能覆盖 Q，渲染损失无惩罚，属"可接受吸收"而非坍缩；如需精确分类，需查 generator 中 Q 场景与 C 的可区分性特征（或接受 C 化）。
2. **L 段准确率 0.261 / A 0.575**：直线被 C 化、弧半对半错。
3. **nseg_gap −1.78**：预测段数系统性偏少（欠分段），40k 长训观察。

## 训练健康度

24k 末段 total ≈ 6.5、mae ≈ 0.049（训练分布）、无 NaN、~3.6-4.0 it/s、梯度范数正常（7-22）。

## 已启动的后续

- 40k 续训后台运行（`runs/newrep_train2.log`，预计 ~02:50 完成）
- 06:40 晨报自动化：40k 终诊 + 部署复测（fp32 19.21MB / demo SVG）+ `MORNING_REPORT.md`
